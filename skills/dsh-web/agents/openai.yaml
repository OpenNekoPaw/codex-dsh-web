interface:
  display_name: "DSH Web"
  short_description: "Drive DSH Web sessions from Codex"
  default_prompt: "Use $dsh-web to delegate this task to DSH Web, open its UI in the built-in browser when requested, inspect its changes, and verify the result."
