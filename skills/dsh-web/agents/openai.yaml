interface:
  display_name: "DSH Web"
  short_description: "Drive DSH Web sessions from Codex"
  default_prompt: "Use $dsh-web for this task, show the exact active DSH session when requested, then inspect and verify the result."
